package com.schulte.grid

import android.app.Application

/**
 * Application 类 —— 应用入口
 */
class SchulteApp : Application() {

    override fun onCreate() {
        super.onCreate()
        // 可在此初始化全局组件
    }
}
